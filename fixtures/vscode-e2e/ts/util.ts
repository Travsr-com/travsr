// FROZEN FIXTURE - do not reformat. expected.json pins line numbers.
// Case: `format` is defined here, in report.ts, and in py/pipeline.py.
// Resolution must disambiguate by file instead of collapsing all three.
export function format(n: number): string {
  return `#${n}`;
}
