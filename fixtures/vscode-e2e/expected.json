{
  "$comment": [
    "HAND-WRITTEN GROUND TRUTH. Do not regenerate this file from travsr output.",
    "A generated expected.json freezes today's bugs as tomorrow's contract and the",
    "suite goes green forever. Every line/path below was read off the fixture source",
    "by a human and must be updated by hand when the fixture changes.",
    "",
    "Line numbers are 1-based and pinned. The fixture files carry a FROZEN header",
    "for that reason - reformatting them invalidates every entry here."
  ],
  "fixtureRoot": "fixtures/vscode-e2e",

  "static": [
    {
      "id": "submit-two-callers",
      "protects": "the ordinary happy path: a method with more than one call site",
      "symbol": "submit",
      "definedAt": "ts/orders.ts:5",
      "callers": ["ts/handlers.ts:6", "ts/jobs.ts:5"],
      "callerCount": 2
    },
    {
      "id": "handleRequest-one-caller",
      "protects": "single-caller counting, and that main.ts is seen as a caller",
      "symbol": "handleRequest",
      "definedAt": "ts/handlers.ts:5",
      "callers": ["ts/main.ts:7"],
      "callerCount": 1
    },
    {
      "id": "main-zero-callers",
      "protects": "zero-caller symbol renders an empty result, not an error or a spinner",
      "symbol": "main",
      "definedAt": "ts/main.ts:6",
      "callers": [],
      "callerCount": 0,
      "mustNot": "error, throw, or report callers borrowed from another symbol"
    },
    {
      "id": "types-no-codelens",
      "protects": "a type-only file gets no CodeLens at all, rather than a wrong '0 callers' lens",
      "file": "ts/types.ts",
      "functionOrMethodNodes": 0,
      "expectedCodeLenses": [],
      "note": "OrderId (line 4) and OrderStatus (line 8) are declarations, not callables."
    },
    {
      "id": "format-three-defs-same-name",
      "protects": "same-name symbols in three files stay distinct instead of collapsing",
      "symbol": "format",
      "definitions": [
        {
          "definedAt": "ts/util.ts:4",
          "callers": ["ts/handlers.ts:7"],
          "callerCount": 1
        },
        {
          "definedAt": "ts/report.ts:5",
          "callers": [],
          "callerCount": 0
        },
        {
          "definedAt": "py/pipeline.py:6",
          "callers": ["py/pipeline.py:11"],
          "callerCount": 1
        }
      ],
      "mustNot": "report 2 callers for any single definition by merging the three",
      "note": "report.ts:5 having 0 while util.ts:4 has 1 is the whole point of this case."
    },
    {
      "id": "orders-blast-radius",
      "protects": "blast radius follows incoming edges only, not the whole import closure",
      "file": "ts/orders.ts",
      "affectedFiles": ["ts/handlers.ts", "ts/jobs.ts", "ts/main.ts"],
      "mustNot": "include ts/types.ts, which orders.ts depends on rather than the reverse"
    },
    {
      "id": "handlers-dependencies",
      "protects": "get_dependencies returns direct imports",
      "file": "ts/handlers.ts",
      "dependencies": ["ts/orders.ts", "ts/util.ts"]
    },
    {
      "id": "ruby-partial-coverage-softening",
      "protects": "#450 / #551 - no definitive zero when the file was never Phase-B analysed",
      "symbol": "legacy_export",
      "definedAt": "rb/legacy.rb:6",
      "precondition": "no Ruby Phase B provider installed (scip-ruby absent)",
      "expect": "softened partial-coverage wording",
      "mustNot": "state or imply 0 references as a definitive answer",
      "note": "If scip-ruby is later installed on the runner this case must be skipped, not failed. Assert the precondition first."
    }
  ],

  "runtime": [
    {
      "id": "ghost-node-after-delete-watcher-live",
      "protects": "the watcher retracts nodes for a deleted file",
      "precondition": "daemon running",
      "procedure": ["index the fixture", "delete ts/jobs.ts", "wait for the watcher", "ask nightlyJob"],
      "expect": "no grounded match; node count drops; fsck clean",
      "status": "VERIFIED PASSING 2026-08-05 (37 -> 34 nodes within ~3s)"
    },
    {
      "id": "ghost-node-after-delete-daemon-stopped",
      "protects": "a stale index is at least detectable when nothing is watching",
      "precondition": "daemon stopped",
      "procedure": ["index the fixture", "stop the daemon", "delete ts/jobs.ts", "ask nightlyJob", "run fsck"],
      "expect": "fsck reports ts/jobs.ts as a ghost path",
      "knownGap": "`ask` still returns fn:nightlyJob at ts/jobs.ts:4 with `confidence: exact` and score 1.000. fsck is the only surface that knows. Not filed: with the daemon running (the VS Code case) this does not occur.",
      "status": "VERIFIED 2026-08-05, fsck half passes"
    },
    {
      "id": "phase-b-edge-loss-on-readd",
      "protects": "a delete and re-add cycle does not silently drop semantic edges",
      "precondition": "daemon running, Phase B complete",
      "procedure": ["index the fixture", "delete ts/jobs.ts", "wait", "restore it", "wait", "count ref/call edges"],
      "expect": "ref/call count returns to 8",
      "status": "VERIFIED FAILING 2026-08-05 - drops to 7 and never recovers without a full reindex. Filed as #583."
    },
    {
      "id": "stale-status-surfaces",
      "protects": "the extension status bar reflects an out-of-date index",
      "precondition": "daemon stopped",
      "procedure": ["index the fixture", "stop the daemon", "append a function to ts/util.ts", "read status"],
      "expect": "status reports stale",
      "knownGap": "`travsr status` output is byte-identical whether or not the index matches the working tree, and fsck reports clean for modified (as opposed to deleted) files. No surface reports staleness. Not filed: with the daemon running the edit is picked up in ~3s.",
      "status": "VERIFIED FAILING 2026-08-05"
    }
  ]
}
