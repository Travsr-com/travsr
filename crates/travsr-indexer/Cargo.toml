[package]
name = "travsr-indexer"
description = "Tree-sitter + LSIF parsing pipeline for Travsr"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
travsr-core = { path = "../travsr-core" }
anyhow = { workspace = true }
# Tree-sitter parsers will be re-added in Sprint 1 once the indexer wires them
# up with a version pair that resolves cleanly (see docs/planning/SPRINT-01.md).
